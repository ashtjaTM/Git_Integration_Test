import { test, expect } from '@playwright/test';

test("Positive: Search plus filter combination shows filtered results", async ({ page }) => {
  // Step 1: Navigate to {{ikea}}/us/en/
  // Step 2: Search for 'test' in the search bar
  // Step 3: Click the filters section
  // Step 4: Select the 'Beds & mattresses' category filter
});
